class Solution:
    def finalValueAfterOperations(self, operations: List[str]) -> int:
        c = 0       
        for op in operations:
            if '+' in op:
                c += 1
            else:
                c -= 1              
        return c
        
