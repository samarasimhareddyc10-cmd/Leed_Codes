class Solution:
    def toLowerCase(self, s: str) -> str:
        res = []
        for char in s:
            if 'A' <= char <= 'Z':
                res.append(chr(ord(char) + 32))
            else:
                res.append(char)
        return "".join(res
